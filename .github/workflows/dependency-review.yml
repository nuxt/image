name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@abdbe14c7671e958053446a3a0be5812d90ce55f # main
    with:
      fail-on-provenance-change: true
