name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@9b91472e8adfdf5d29c9a357e13f594d259a0d47 # main
    with:
      fail-on-provenance-change: true
