name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  check-provenance:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@main
    with:
      fail-on-provenance-change: true
