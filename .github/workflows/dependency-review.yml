name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@adcc028738b85936d35de6ba36911722e69a5e67 # main
    with:
      fail-on-provenance-change: true
