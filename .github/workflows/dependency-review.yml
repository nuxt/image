name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@9092aad206e57eb55447b3e02f2ca932faeebebe # main
    with:
      fail-on-provenance-change: true
