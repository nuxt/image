name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@e32461e7e5e9d4e01426073b831a0dd81b131628 # main
    with:
      fail-on-provenance-change: true
