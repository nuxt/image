name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@b9e5f7758d976e3e88ee08e63e14308ca1e5b0a6 # main
    with:
      fail-on-provenance-change: true
