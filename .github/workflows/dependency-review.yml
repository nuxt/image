name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@fea7e1311a4402022f41cefb99bbeb1d6cdc850a # main
    with:
      fail-on-provenance-change: true
