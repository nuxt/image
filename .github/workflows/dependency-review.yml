name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@2b5b7220348b3f84594cde363cb6eb44d1379095 # main
    with:
      fail-on-provenance-change: true
