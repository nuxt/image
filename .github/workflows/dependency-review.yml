name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@b400fce588501ddafef51149a92a41406307bcce # main
    with:
      fail-on-provenance-change: true
