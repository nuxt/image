name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@512f2cf8ca0dd7ad8cbfa393ac1a7d62f8a73049 # main
    with:
      fail-on-provenance-change: true
