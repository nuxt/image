name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    uses: nuxt/.github/.github/workflows/dependency-review.yml@5e4a873fc9ee6d0a7096c5eba1eea6339feef793 # main
    with:
      fail-on-provenance-change: true
